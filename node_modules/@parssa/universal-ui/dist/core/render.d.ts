import React from "react";
import { Props } from "./types";
export declare function forwardRefWithAs<T extends {
    name: string;
    displayName?: string;
}>(component: T): T & {
    displayName: string;
};
export declare function render<TTag extends React.ElementType>({ props, defaultTag }: {
    props: Props<TTag>;
    defaultTag: React.ElementType;
}): React.ReactElement<any, string | ((props: any) => React.ReactElement<any, any> | null) | (new (props: any) => React.Component<any, any, any>)>;
