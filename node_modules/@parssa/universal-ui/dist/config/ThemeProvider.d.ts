import React from 'react';
import { Props } from '../core';
import { Size, Theme } from '../types';
export interface ThemeProviderProps {
    theme?: Theme;
    size?: Size;
    dark?: boolean;
    inverted?: boolean;
}
export declare const ThemeProvider: (<TTag extends React.ElementType<any> = "div">(props: Pick<TTag extends React.ElementType<any> ? React.ComponentProps<TTag> : never, Exclude<keyof (TTag extends React.ElementType<any> ? React.ComponentProps<TTag> : never), "as" | "children" | "className">> & {
    as?: TTag | undefined;
    children?: React.ReactNode;
} & ((TTag extends React.ElementType<any> ? React.ComponentProps<TTag> : never) extends {
    className?: any;
} ? {
    className?: string | undefined;
} : {}) & ThemeProviderProps, ref: React.Ref<TTag>) => React.ReactElement<any, string | ((props: any) => React.ReactElement<any, any> | null) | (new (props: any) => React.Component<any, any, any>)>) & {
    displayName: string;
};
